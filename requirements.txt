# PreSnap dependencies — pinned for reproducible dev / CI / container builds.
# Unpinned deps are a silent-results risk: the container could resolve different
# versions than dev and produce different numbers.
pytest==9.1.1

# Phase 1 — ingestion + storage
nflreadpy==0.1.5
polars==1.44.2
psycopg[binary]==3.3.5

# Phase 3 — modeling
scikit-learn==1.9.0
joblib==1.6.0
numpy==2.5.3

# Phase 4 — evaluation + calibration
matplotlib==3.11.2
